declare namespace _default {
    export { trace };
    export { debug };
    export { info };
    export { warn };
    export { error };
    export { setupGoogleCloud };
    export { formatRequest };
    export { middleware };
    export { useConsole };
    export { useFormatted };
    export { useGoogleCloud };
}
export default _default;
import { trace } from "./logger";
import { debug } from "./logger";
import { info } from "./logger";
import { warn } from "./logger";
import { error } from "./logger";
/**
 * @param {Object} [options]
 * @param {boolean} [options.logging=true]
 * @param {boolean|Object} [options.tracing=true]
 */
export function setupGoogleCloud(options?: {
    logging?: boolean;
    tracing?: boolean | any;
}): void;
import { formatRequest } from "./logger";
import middleware from "./middleware";
import { useConsole } from "./logger";
import { useFormatted } from "./logger";
import { useGoogleCloud } from "./logger";
export { trace, debug, info, warn, error, formatRequest, middleware, useConsole, useFormatted, useGoogleCloud };
//# sourceMappingURL=index.d.ts.map